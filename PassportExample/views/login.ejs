<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewpointer" content="width=device-width,height = device-height, initial-scale= 1">
		<title>로그인 화면</title>
	</head>
</html>
<body>
	<div class="container">
		<h1>로그인</h1>
		<!-- 인증 처리 후 메시지가 있으면 메시지 표시 -->
		<% if (message.length>0) {%>
		<div><%=message %></div>
		<%}%>
		<form action="/login" method="post">
			<div>
			<label>이메일</label>
			<input type="text" name="email">
			</div>
			<div>
				<label>비밀번호</label>
				<input type="password" name="password">
			</div>
			<button type="submit" >로그인</button>
		</form>
		<hr>
		<p>계정이 없으세요? <a href="/signup">회원가입하기</a></p>
		<p><a href="/">홈으로</a></p>
	</div>
</body>






















